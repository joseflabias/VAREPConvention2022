import { PureComponent, ReactNode } from 'react';
import { ScrollViewProps } from 'react-native';
export interface HorizontalPickerProps extends ScrollViewProps {
    data: any[];
    renderItem: (item: any, index: number) => ReactNode;
    itemWidth: number;
    defaultIndex?: number;
    animatedScrollToDefaultIndex?: boolean;
    snapTimeout?: number;
    onChange?: (position: number) => void;
}
export declare type HorizontalPickerState = {
    scrollViewWidth: number;
};
export default class HorizontalPicker extends PureComponent<HorizontalPickerProps, HorizontalPickerState> {
    private paddingSide;
    private refScrollView;
    private ignoreNextScroll;
    private timeoutDelayedSnap;
    private currentPositionX;
    private readonly defaultScrollEventThrottle;
    private readonly defaultDecelerationRate;
    private readonly defaultSnapTimeout;
    constructor(props: HorizontalPickerProps);
    private onLayoutScrollView;
    private onScroll;
    private onScrollBeginDrag;
    private onScrollEndDrag;
    private onMomentumScrollBegin;
    private onMomentumScrollEnd;
    scrollToPosition: (position: number) => void;
    private cancelDelayedSnap;
    private setDelayedSnap;
    scrollToDefaultIndex: () => void;
    render(): JSX.Element;
}
